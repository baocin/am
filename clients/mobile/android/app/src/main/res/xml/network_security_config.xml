<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext traffic to localhost for development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain> <!-- Android emulator host -->
    </domain-config>

    <!-- Allow cleartext for local network development (physical devices) -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Add your local development machine IP here -->
        <!-- Example: <domain includeSubdomains="false">192.168.1.100</domain> -->
        <domain includeSubdomains="false">100.94.14.44</domain>
        <!-- Common local network IPs -->
        <domain includeSubdomains="false">192.168.1.1</domain>
        <domain includeSubdomains="false">192.168.0.1</domain>
    </domain-config>

    <!-- Default config for all other domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
